{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "turbo run start",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "eslint \"src/**/*.ts*\"",
    "clean": "turbo run clean",
    "server": "turbo run server"
  },
  "devDependencies": {
    "turbo": "latest",
    "eslint": "^8.4.1"
  }
}