{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "turbo run start",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "server": "turbo run server"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
